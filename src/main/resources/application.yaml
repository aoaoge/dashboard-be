server:
  port: 8081
#配置数据库连接
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library?serverTimezone=GMT%2b8  # beijing时间
    username: root
    password: zhangdeao123


#需要配置xml路径  classpath对应的就是resource 如果不配置的话mapper会找不到xml在哪
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


